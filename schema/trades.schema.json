{
    "title": "Trades Schema",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "$defs": {
        "variableInteger": {
            "oneOf": [
                {"type": "integer"},
                {
                    "type": "object",
                    "properties": {
                        "min": {"type": "integer"},
                        "max": {"type": "integer"}
                    },
                    "required": ["min"]
                }
            ]
        },
        "item": {
            "type": "object",
            "properties": {
                "material": {"type": "string"},
                "amount": {"$ref": "#/$defs/variableInteger"},
                "name": {"type": "string"},
                "lore": {
                    "type": "array",
                    "items": {"type": "string"}
                },
                "enchantments": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "enchantment": {"type": "string"},
                            "level": {"$ref": "#/$defs/variableInteger"}
                        },
                        "required": ["enchantment", "level"]
                    }
                }
            },
            "required": ["material"]
        }
    },
    "properties": {
        "null_weight": {"type": "integer"},
        "num_iterations": {"$ref": "#/$defs/variableInteger"},
        "trades": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "weight": {"type": "integer"},
                    "allow_duplicates": {"type": "boolean"},
                    "max_uses": {"$ref": "#/$defs/variableInteger"},
                    "result": {"$ref": "#/$defs/item"},
                    "ingredients": {
                        "type": "array",
                        "maxItems": 2,
                        "items": {"$ref": "#/$defs/item"}
                    }
                },
                "required": ["weight", "result", "ingredients"]
            }
        }
    },
    "required": ["trades"]
}